name: Deploy Supabase

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    # This workflow auto-deploys Supabase migrations and edge functions to production. 
    #
    # When it runs:
    # - Triggers after the CI workflow completes on the main branch
    # - Only proceeds if CI passed (workflow_run.conclusion == 'success')
    # - Only proceeds if it was a 'push' event (not a PR)
    #
    # Why check for 'push'?
    # - PRs to main trigger CI but shouldn't deploy
    # - When a PR is merged, GitHub fires a 'push' event to main
    # - Since main is protected, direct pushes aren't allowed anyway
    # - So 'push' effectively means "a PR was just merged"
    #
    # The workflow checks if migrations or functions changed before
    # deploying, to avoid unnecessary deployments.
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Check for changes
        id: changes
        run: |
          # Get the commits from the triggering workflow
          git fetch origin main --depth=2
          if git diff --name-only HEAD~1 HEAD | grep -q '^supabase/migrations/'; then
            echo "has_migrations=true" >> $GITHUB_OUTPUT
          fi
          if git diff --name-only HEAD~1 HEAD | grep -q '^supabase/functions/'; then
            echo "has_functions=true" >> $GITHUB_OUTPUT
          fi

      - uses: supabase/setup-cli@v1
        if: steps.changes.outputs.has_migrations == 'true' || steps.changes.outputs.has_functions == 'true'
        with:
          version: latest

      - name: Link project
        if: steps.changes.outputs.has_migrations == 'true' || steps.changes.outputs.has_functions == 'true'
        run: supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Push migrations
        if: steps.changes.outputs.has_migrations == 'true'
        run: supabase db push
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy edge functions
        if: steps.changes.outputs.has_functions == 'true'
        run: supabase functions deploy --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
